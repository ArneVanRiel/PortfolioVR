# TABLE AANMAKEN

CREATE TABLE Gebruikers (
    gebruikerId INT PRIMARY KEY,
    gebruikersnaam VARCHAR(255) UNIQUE,
    email VARCHAR(255),
    wachtwoord VARCHAR(255)
);

# COLUMN TOEVOEGEN

ALTER TABLE Gebruikers ADD COLUMN email VARCHAR(255);
